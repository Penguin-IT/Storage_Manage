<UserControl x:Class="Storage_Manage.Views.TimKiemUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="1000">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Tìm Kiếm Hoá Đơn" Margin="10" Padding="10">
            <WrapPanel Orientation="Horizontal">
                <StackPanel Margin="5">
                    <TextBlock Text="Tiêu chí:"/>
                    <ComboBox Width="150" ItemsSource="{Binding SearchCriteria}" 
                              SelectedItem="{Binding SelectedCriteria}"/>
                </StackPanel>

                <StackPanel Margin="5">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsTimeSearchSelected}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <TextBlock Text="Từ khóa:"/>
                    <TextBox Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" 
                            Visibility="{Binding IsTimeSearchSelected, Converter={StaticResource BoolToVis}}">
                    <StackPanel Margin="5">
                        <TextBlock Text="Từ ngày:"/>
                        <DatePicker SelectedDate="{Binding FromDate}"/>
                    </StackPanel>

                    <StackPanel Margin="5">
                        <TextBlock Text="Đến ngày:"/>
                        <DatePicker SelectedDate="{Binding ToDate}"/>
                    </StackPanel>
                </StackPanel>

                <Button Content="Tìm kiếm" Width="100" Height="35" Margin="5,15,0,0"
                        Background="SkyBlue" Command="{Binding SearchCommand}"/>
            </WrapPanel>
        </GroupBox>

        <DataGrid Grid.Row="1" Margin="10" AutoGenerateColumns="False" 
                  ItemsSource="{Binding OutputList}" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã Phiếu" Binding="{Binding MaPN}" Width="100"/>
                <DataGridTextColumn Header="Ngày Nhập" Binding="{Binding NgayNhap, StringFormat=\{0:dd/MM/yyyy\}}" Width="150"/>
                <DataGridTextColumn Header="Nhà Cung Cấp" Binding="{Binding NhaCungCap.TenNCC}" Width="*"/>
                <DataGridTextColumn Header="Nhân Viên Lập" Binding="{Binding NhanVien.HoTen}" Width="*"/>
                <DataGridTextColumn Header="Tổng Tiền" Binding="{Binding TongTien, StringFormat='{}{0:N0} VNĐ'}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>